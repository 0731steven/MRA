# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Database
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
alembic>=1.14.0
# asyncpg>=0.30.0  # uncomment for production PostgreSQL

# Auth
pyjwt>=2.10.0

# Feishu
lark-oapi>=1.3.5

# LLM
openai>=1.60.0

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.9
httpx>=0.28.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── Pipeline scripts (scripts/) ────────────────────────────────────────────
# PDF parsing / metadata
pymupdf>=1.24.0            # imported as `fitz` (local_search)
pymupdf4llm>=0.0.17        # PDF→MD conversion (ingest_pdf, patent_convert, web_ingest)
pyyaml>=6.0               # frontmatter parsing
markdownify>=0.13.0       # web_ingest HTML→MD
# Browser automation for IEEE / CNIPA / Scholar search (needs Chrome on CDP :9222)
playwright>=1.60.0        # Chrome 148+ requires ws:// CDP URL (not http://); 1.60+ handles this correctly
